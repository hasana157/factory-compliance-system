# Factory Compliance Dashboard

React dashboard for live factory compliance monitoring.

```bash
npm install
npm run dev
```

The dashboard expects the FastAPI backend at `VITE_API_URL` or `http://localhost:8000`.
